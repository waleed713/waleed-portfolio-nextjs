# --- Sanity CMS (Blog) ---
# Create a free project at https://www.sanity.io/manage
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
# Only needed if you enable "draft mode" / private datasets
SANITY_API_READ_TOKEN=

# --- Site ---
# Used to build absolute URLs for metadata, sitemap.xml and robots.txt
NEXT_PUBLIC_SITE_URL=https://waleedahmad.dev

# --- Contact form (optional, if you wire the form to an email service) ---
# e.g. Resend, EmailJS, or Formspree endpoint
RESEND_API_KEY=
CONTACT_TO_EMAIL=waleedaw778@gmail.com
